---
name: 'step-v-07-implementation-leakage-validation'
description: '实现泄露检查 - 确保 FR 和 NFR 不包含实现细节'

# 文件引用（仅此步骤使用的变量）
nextStepFile: './step-v-08-domain-compliance-validation.md'
prdFile: '{prd_file_path}'
validationReportPath: '{validation_report_path}'
---

# 步骤 7：实现泄露验证

## 步骤目标：

确保功能需求和非功能需求不包含实现细节 - 它们应该指定做什么（WHAT），而不是如何做（HOW）。

## 强制执行规则（首先阅读）：

### 通用规则：

- 🛑 永远不要在没有用户输入的情况下生成内容
- 📖 关键：在采取任何行动之前，始终阅读完整的步骤文件
- 🔄 关键：当使用 'C' 加载下一步时，确保整个文件已被阅读
- 📋 您是引导者，不是内容生成器
- ✅ 您必须始终使用配置的 `{communication_language}` 以您的 Agent 沟通风格输出

### 角色强化：

- ✅ 您是验证架构师和质量保证专员
- ✅ 如果您已被赋予沟通或人设模式，请在扮演此新角色的同时继续使用它们
- ✅ 我们进行系统化验证，不是协作对话
- ✅ 您带来分析严谨性和关注点分离专业知识
- ✅ 此步骤自动运行 - 无需用户输入

### 步骤特定规则：

- 🎯 仅专注于实现泄露检测
- 🚫 禁止在此步骤中验证其他方面
- 💬 方法：系统化扫描技术和实现术语
- 🚪 这是验证序列步骤 - 完成后自动继续

## 执行协议：

- 🎯 扫描 FR 和 NFR 中的实现术语
- 💾 区分能力相关与泄露
- 📖 将发现追加到验证报告
- 📖 显示"正在进行下一个检查..."并加载下一步
- 🚫 禁止暂停或请求用户输入

## 上下文边界：

- 可用上下文：PRD 文件，验证报告
- 焦点：仅实现泄露检测
- 限制：不验证其他方面，不暂停用户输入
- 依赖：步骤 2-6 已完成 - 初始验证完成

## 强制序列

**关键：** 完全按此序列执行。除非用户明确请求更改，否则不要跳过、重新排序或即兴发挥。

### 1. 尝试子流程验证

**尝试使用 Task 工具启动子流程：**

"对此 PRD 执行实现泄露验证：

**扫描：**
1. 技术名称（React、Vue、Angular、PostgreSQL、MongoDB、AWS、GCP、Azure、Docker、Kubernetes 等）
2. 库名称（Redux、axios、lodash、Express、Django、Rails、Spring 等）
3. 数据结构（JSON、XML、CSV）除非与能力相关
4. 架构模式（MVC、微服务、无服务器）除非是业务需求
5. 协议名称（HTTP、REST、GraphQL、WebSockets）- 检查是否能力相关

**对于找到的每个术语：**
- 这是能力相关的吗？（例如，'API 消费者可以访问...' - API 是能力）
- 还是这是实现细节？（例如，'React 组件用于...' - 实现）

记录违规及行号和解释。

返回带有泄露计数和示例的结构化发现。"

### 2. 优雅降级（如果 Task 工具不可用）

如果 Task 工具不可用，直接执行分析：

**要扫描的实现泄露术语：**

**前端框架：**
React、Vue、Angular、Svelte、Solid、Next.js、Nuxt 等。

**后端框架：**
Express、Django、Rails、Spring、Laravel、FastAPI 等。

**数据库：**
PostgreSQL、MySQL、MongoDB、Redis、DynamoDB、Cassandra 等。

**云平台：**
AWS、GCP、Azure、Cloudflare、Vercel、Netlify 等。

**基础设施：**
Docker、Kubernetes、Terraform、Ansible 等。

**库：**
Redux、Zustand、axios、fetch、lodash、jQuery 等。

**数据格式：**
JSON、XML、YAML、CSV（除非能力相关）

**对于 FR/NFR 中找到的每个术语：**
- 确定是能力相关还是实现泄露
- 示例："API 消费者可以通过 REST 端点访问数据" - API/REST 是能力
- 示例："React 组件使用 Redux 获取数据" - 实现泄露

**统计违规并记录行号**

### 3. 统计实现泄露

**按类别：**
- 前端框架泄露：计数
- 后端框架泄露：计数
- 数据库泄露：计数
- 云平台泄露：计数
- 基础设施泄露：计数
- 库泄露：计数
- 其他实现细节：计数

**总实现泄露违规：** 总和

### 4. 向验证报告报告实现泄露发现

追加到验证报告：

```markdown
## 实现泄露验证

### 按类别的泄露

**前端框架：** {计数} 处违规
{如果有违规，列出带行号的示例}

**后端框架：** {计数} 处违规
{如果有违规，列出带行号的示例}

**数据库：** {计数} 处违规
{如果有违规，列出带行号的示例}

**云平台：** {计数} 处违规
{如果有违规，列出带行号的示例}

**基础设施：** {计数} 处违规
{如果有违规，列出带行号的示例}

**库：** {计数} 处违规
{如果有违规，列出带行号的示例}

**其他实现细节：** {计数} 处违规
{如果有违规，列出带行号的示例}

### 摘要

**总实现泄露违规：** {总计}

**严重程度：** [如果 >5 个违规为严重，2-5 个为警告，<2 个为通过]

**建议：**
[如果严重] "发现大量实现泄露。需求指定了如何做而不是做什么。移除所有实现细节 - 这些属于架构，不是 PRD。"
[如果警告] "检测到一些实现泄露。审查违规并从需求中移除实现细节。"
[如果通过] "未发现显著的实现泄露。需求正确地指定了做什么而不是如何做。"

**注意：** API 消费者、GraphQL（当需要时）和其他能力相关术语在描述系统必须做什么（不是如何构建）时是可以接受的。
```

### 5. 显示进度并自动继续

显示："**实现泄露验证完成**

总违规数：{计数}（{严重程度}）

**正在进行下一个验证检查...**"

立即完整阅读并遵循：{nextStepFile}（step-v-08-domain-compliance-validation.md）

---

## 系统成功/失败指标

### 成功：

- 扫描了 FR 和 NFR 中的所有实现术语类别
- 区分了能力相关与实现泄露
- 违规已记录行号和解释
- 严重程度已正确评估
- 发现已报告到验证报告
- 自动进入下一个验证步骤
- 已尝试子流程并优雅降级

### 失败：

- 未扫描所有实现术语类别
- 未区分能力相关与泄露
- 缺少违规的行号
- 未向验证报告报告发现
- 未自动继续

**主规则：** 需求指定做什么（WHAT），不是如何做（HOW）。实现细节属于架构文档，不是 PRD。